"""Service layer: business logic behind per-feature interfaces.

Services depend on repository and utility *interfaces* (not implementations),
keeping them decoupled from persistence and easy to test in isolation.
"""
